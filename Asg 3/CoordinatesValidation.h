// BY ANKITA KAPOOR
// Student ID : 20811694
#include <stdlib.h>
#include <iostream>
#include <vector>
using namespace std;

class CoordinatesValidation {
public:
	double getDist(int, int, int, int);
	bool ptInCoordinates(vector<int> coord, int, int);
	bool samePt(vector<int> coord, int, int);
};

