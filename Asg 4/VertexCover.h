#include <vector>
#include <iostream>

using namespace std;


class VertexCover {
public:
	vector<int> negCombi(int, vector<int>, int, int);
	int combi(int);
	void minVertexCov(int, int, vector<string>);
};